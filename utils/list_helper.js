// utils/list_helper.js
const dummy = (blogs) => {
    // The 'blogs' parameter is received but not used in this function yet.
    // The function simply returns the value 1.
    return 1
  }
  
  module.exports = {
    dummy
  }